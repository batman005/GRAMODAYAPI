{
  "name": "gramodayapi",
  "version": "1.0.0",
  "description": "Captures user contributed reports and returns an aggregate report in response.",
  "main": "server.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production && node server.js",
    "dev": "cross-env NODE_ENV=development && nodemon server.js",
    "test": "mocha --timeout 10000",
    "watch": "mocha --watch --timeout 10000"
  },
  "author": "batman 005",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.4",
    "cross-env": "^7.0.3",
    "dotenv": "^10.0.0",
    "express": "^4.17.2",
    "express-mongo-sanitize": "^2.1.0",
    "helmet": "^4.6.0",
    "hpp": "^0.2.3",
    "mongoose": "^6.1.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "chai-http": "^4.3.0",
    "mocha": "^9.1.3"
  }
}
